@model Subsogator.Web.Models.Countries.ViewModels.DeleteCountryViewModel

@{
    ViewData["Title"] = "Delete";
}

<h3>@ViewData["Title"]</h3>

<h3>Are you sure you want to delete this country?</h3>
<div>
    <h4>Country</h4>
    <hr />
    <dl class="row bg-secondary">
        <dt class = "col-sm-2 text-dark bg-light">
            @Html.DisplayNameFor(country => country.Name)
        </dt>
        <dd class = "col-sm-10 text-white">
            @Html.DisplayFor(country => country.Name)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a asp-action="Index">Back to List</a>
    </form>
</div>
