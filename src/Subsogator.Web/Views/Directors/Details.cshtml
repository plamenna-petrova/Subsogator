@model Subsogator.Web.Models.Directors.ViewModels.DirectorDetailsViewModel

@{
    ViewData["Title"] = "Director Details";
}

<h3>@ViewData["Title"]</h3>

<div>
    <hr />
    <dl class="row bg-secondary">
        <dt class="col-sm-2 text-white bg-info font-weight-normal">
            @Html.DisplayNameFor(director => director.FirstName)
        </dt>
        <dd class="col-sm-10 text-white">
            @Html.DisplayFor(director => director.FirstName)
        </dd>
        <dt class="col-sm-2 text-white bg-info font-weight-normal">
            @Html.DisplayNameFor(director => director.LastName)
        </dt>
        <dd class="col-sm-10 text-white">
            @Html.DisplayFor(director => director.LastName)
        </dd>
        <dt class="col-sm-2 text-white bg-info font-weight-normal">
            @Html.DisplayNameFor(director => director.CreatedOn)
        </dt>
        <dd class="col-sm-10 text-white">
            @Html.DisplayFor(director => director.CreatedOn)
        </dd>
        <dt class="col-sm-2  text-white bg-info font-weight-normal">
            @Html.DisplayNameFor(director => director.ModifiedOn)
        </dt>
        <dd class="col-sm-10 text-white">
            @Html.DisplayFor(director => director.ModifiedOn)
        </dd>
        <dt class="col-sm-2 text-white bg-info font-weight-normal">
            Related Film Productions
        </dt>
        <dd class="col-sm-10 text-white">
            @if (Model.RelatedFilmProductions.Count() > 0)
            {
                <table class="table table-striped table-light table-hover">
                    <thead>
                        <tr>
                            <th>Title</th>
                            <th>Duration</th>
                            <th>Release Date</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var filmProduction in Model.RelatedFilmProductions)
                        {
                            <tr>
                                <td>
                                    @filmProduction.Title
                                </td>
                                <td>
                                    @filmProduction.Duration
                                </td>
                                <td>
                                    @filmProduction.ReleaseDate.ToShortDateString()
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
            }
            else
            {
                <div>No Related Film Productions</div>
            }
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Index" class="btn btn-primary">Back To Directors List</a>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-dark">Edit Director</a>
</div>
